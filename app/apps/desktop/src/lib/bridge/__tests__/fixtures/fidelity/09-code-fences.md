# Code fences

```ts
const greeting: string = "hello";
function add(a: number, b: number): number {
  return a + b;
}
```

```python
def f(n):
    return [x * 2 for x in range(n)]  # comment
```

A fence containing backticks needs a longer fence:

````markdown
```js
console.log("nested");
```
````

Indented code:

    four spaces make a code block

A plain fence with no language:

```
just text in a fence
```
